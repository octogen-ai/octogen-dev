# Build output and dependencies
coverage
dist
node_modules

# Lockfiles are tool-owned
package-lock.json
uv.lock

# Python tooling owns its own formatting (ruff)
sdks/python

# Emitted by `npm run codegen`, not hand-formatted
**/src/generated
tests/contract/openapi
